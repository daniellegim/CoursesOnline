import { createStyles, makeStyles } from '@mui/styles'

export const useStyles = makeStyles((theme) =>
    createStyles({
        fab: {
            // position: "absolute",
            // top: 16,
            // left: 16,
        }
    })
);
